<!DOCTYPE html>
<html>
    <head>
        <title>Fountains of UChicago</title>
        <%= stylesheet_link_tag    "application", :media => "all" %>
        <%= javascript_include_tag "application" %>
        <%= csrf_meta_tags %>
    </head>
    <body>

        <% if user_signed_in? %>
            Signed in as <%= link_to current_user.name, edit_user_registration_path %>
                (<%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %>)
        <% else %>
            <%= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook) %>
        <% end %>

        <h1><a href="/">HydRater&trade; v.5.0!!!</a></h1>

        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>

        <%= yield %>

        <footer>brought to you by the software scavgeneers of <strong>Bill Nye and the Science Lies</strong></footer>
    </body>
</html>
